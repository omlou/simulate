{
  "scripts": {
    "update": "cross-env NODE_ENV=development && rollup -c",
    "commit": "gulp simplify --host %npm_config_host% && npm publish --access public & gulp dev",
    "start": "gulp serve",
    "push": "gulp push --host %npm_config_host% --port %npm_config_port%"
  },
  "devDependencies": {
    "gulp-webserver": "^0.9.1"
  },
  "dependencies": {
    "@xlou/simulate": "^1.0.6",
    "axios": "^1.4.0",
    "clear-ajax": "^1.0.0"
  }
}