{
  "scripts": {
    "start": "gulp serve",
    "update": "cross-env NODE_ENV=development rollup -c",
    "commit": "cross-env TYPE=commit gulp simplify&&npm publish --access public&&gulp dev",
    "upgrade": "cross-env TYPE=upgrade gulp simplify&&npm publish --access public&&gulp dev"
  },
  "devDependencies": {
    "gulp-webserver": "^0.9.1"
  },
  "dependencies": {
    "@xlou/simulate": "^1.0.6",
    "axios": "^1.4.0",
    "clear-ajax": "^1.0.0"
  }
}